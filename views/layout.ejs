<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameGaleria</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <link rel="stylesheet" href="/stylesheets/layout.css">
    <link rel="stylesheet" href="/stylesheets/games.css">
</head>
<body>
    <nav class="navbar navbar-dark">
        <div class="container-fluid px-4">
            <a class="navbar-brand fs-2 brand-title" href="/">GameGaleria</a>
            <% if (locals.user) { %>
                <div class="d-flex align-items-center">
                    <span class="me-3 text-secondary">ID: <%= user.name %></span>
                    <a href="/logout" class="btn btn-outline-danger btn-sm">LOGOUT</a>
                </div>
            <% } %>
        </div>
    </nav>

    <main>
        <div class="full-viewport">
            <%- body %>
        </div>
    </main>

    <footer class="bg-black text-secondary py-3 border-top border-secondary text-center mt-auto">
        <small class="brand-title" style="font-size: 0.7rem;">GameGaleria 2026</small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>